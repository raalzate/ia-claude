{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ia-claude.local/schemas/kata-008/memory-entry.schema.json",
  "title": "MemoryEntry",
  "description": "One resolved memory rule (FR-001, Principle VIII). Scope distinguishes team rules (win on project tasks) from personal rules.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "content", "scope", "source_path"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "content": { "type": "string", "minLength": 1 },
    "scope": { "type": "string", "enum": ["team", "personal"] },
    "source_path": {
      "type": "string",
      "description": "Relative or absolute path of the file the entry was resolved from."
    },
    "order": {
      "type": "integer",
      "minimum": 0,
      "description": "Deterministic load order across a fresh clone (SC-001 reproducibility)."
    }
  }
}
