{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ia-claude.local/schemas/kata-011/critical-rule.schema.json",
  "title": "CriticalRule",
  "description": "A rule that MUST sit at prompt edges to survive mid-context attention dilution (FR-001, FR-002).",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "content", "length_tokens", "priority"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "content": { "type": "string", "minLength": 1 },
    "length_tokens": { "type": "integer", "minimum": 1 },
    "priority": {
      "type": "integer",
      "minimum": 0,
      "description": "Lower wins when competing for edge budget."
    },
    "placement": {
      "type": "string",
      "enum": ["prefix", "suffix", "both"],
      "description": "Which edge(s) this rule must appear at. Default 'both'."
    }
  }
}
