{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ia-claude.local/schemas/kata-011/compaction-event.schema.json",
  "title": "CompactionEvent",
  "description": "Fires when context usage crosses the declared threshold (default 0.55). Post-event prompt MUST re-anchor every CriticalRule at its declared edge (FR-003, SC-003, SC-004).",
  "type": "object",
  "additionalProperties": false,
  "required": ["session_id", "triggered_at", "usage_ratio", "threshold", "rules_reanchored"],
  "properties": {
    "session_id": { "type": "string" },
    "triggered_at": { "type": "string", "format": "date-time" },
    "usage_ratio": { "type": "number", "minimum": 0, "maximum": 1 },
    "threshold": { "type": "number", "minimum": 0, "maximum": 1 },
    "rules_reanchored": {
      "type": "array",
      "items": { "type": "string" },
      "description": "CriticalRule.id values confirmed present in the post-compaction prompt."
    },
    "content_compacted_bytes": { "type": "integer", "minimum": 0 }
  }
}
